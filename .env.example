# Database
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"
BETTER_AUTH_SECRET="your-secret-key-here"
# Canonical app URL for auth callbacks and server-side redirects.
# BETTER_AUTH_URL="https://your-domain.com"
# Optional extra trusted origins (comma-separated), useful for preview deploys.
# BETTER_AUTH_TRUSTED_ORIGINS="https://deploy-preview-123--your-site.netlify.app,https://staging.example.com"
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""


R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""

VITE_R2_BUCKET=""
VITE_R2_ENDPOINT=""
# Optional client auth endpoint override. Default is same-origin.
# Set to "same-origin" (or leave unset) for local + Netlify deploys.
# VITE_AUTH_CLIENT_BASE_URL="same-origin"
# Use an absolute URL only if your auth API is hosted on another origin.
# VITE_AUTH_CLIENT_BASE_URL="https://auth.example.com"
VITE_STRIPE_BASIC_PRICE_ID="price_..."
VITE_STRIPE_PRO_PRICE_ID="price_..."
VITE_STRIPE_PUBLISHABLE_KEY="pk_test_..."
